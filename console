#!/usr/bin/env php
<?php

declare(strict_types=1);

use App\Http\ErrorHandler;
use App\Services\SystemAdministration\OpenStreetMap\OpenStreetMapSync;
use Framework\Console\ConsoleKernel;

include 'vendor/autoload.php';

if (in_array(($argv[1] ?? ''), ['--interactive', '-it'])) {
    passthru('php -d auto_prepend_file="vendor/autoload.php" -a');
    exit;
}

$app = app();

try {
    // Must be invokable / handle(); Container::make() calls bindings with 0 args (never pass $e here).
    $app->bind('errorHandler', ErrorHandler::class, true);
    $kernel = $app->get(ConsoleKernel::class);

    $kernel->loadCommands(path: 'app/Console/Commands');
    $kernel->withCommand(OpenStreetMapSync::class);

    $kernel->handle();
} catch (Error | Exception | Throwable $e) {
    if (env('DEBUG')) {
        throw $e;
    }
    app()->handleError($e);
}
